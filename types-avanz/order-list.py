numbers = [1, 22, 13, 4, 5, 2, 0]
numbers.sort()
print(numbers)
numbersSort = sorted(numbers)
print(numbersSort)

users = users = [['user', 3], ['user', 1], ['user', 2]]
users.sort(key=lambda x: x[1])
print(users)
